apiVersion: v1
kind: Namespace
metadata:
  name: api2-app

---
apiVersion: v1
kind: Pod
metadata:
  name: api2-app
  namespace: api2-app
  labels:
    app: api2-app
spec:
  containers:
  - name: api2-app
    image:  prateekkumawat/app:api-1.2
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 8000

---
apiVersion: v1
kind: Service
metadata:
  name: api2-app-svc
  namespace: api2-app
spec:
  selector:
    app: api2-app
  ports:
  - port: 80
    targetPort: 8000

# ---
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: api2-app-ingress
#   namespace: api2-app
#   annotations:
#     konghq.com/strip-path: "true"
#     konghq.com/protocols: "http"
# spec:
#   ingressClassName: kong
#   rules:
#   - host: localapp.domain2.example.com
#     http:
#       paths:
#       - path: /api/v2
#         pathType: Prefix
#         backend:
#           service:
#             name: api2-app-svc
#             port:
#               number: 80

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api2-app-ingress
  namespace: api2-app
  annotations:
    konghq.com/strip-path: "true"
    konghq.com/protocols: "https"
spec:
  ingressClassName: kong
  tls: 
   - hosts: 
      - localapp.domain2.example.com
     secretName: kong-tls
  rules:
  - host: localapp.domain2.example.com
    http:
      paths:
      - path: /api/v2
        pathType: Prefix
        backend:
          service:
            name: api2-app-svc
            port:
              number: 80              